---
- title: Tune each model's thinking effort and context size right from the composer
  type: feature-add
  author: aide
  description:
    - Added inline controls next to the model picker to set thinking effort and context size, laid out horizontally like GitHub Copilot
    - Each model now shows exactly the options it actually supports — for example GPT-5.5 offers a None level while Claude Opus offers Max — instead of a single one-size-fits-all list
    - The context-size control only appears for models that have an extended-context tier, and each tier shows its real token window
    - Your choices are remembered per model and applied automatically to new conversations
---
